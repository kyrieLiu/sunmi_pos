<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data class="ItemEmployeeNameBinding">

        <variable
            name="itemViewModel"
            type="ys.app.petproject.viewmodel.manage.EmployeeNameItemViewModel" />
    </data>

    <LinearLayout
        android:id="@+id/root_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <Button
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginBottom="5dp"
            android:layout_marginLeft="10dp"
            android:layout_marginRight="10dp"
            android:layout_marginTop="5dp"
            android:background="@drawable/selector_press_button_normal"
            android:text="@{itemViewModel.model.name}"
            android:onClick="@{itemViewModel.onClickButton}"
            android:textColor="@color/white"
            android:textSize="15sp" />

    </LinearLayout>
</layout>