<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data class="ItemEmployeeNameNewBinding">

        <variable
            name="itemViewModel"
            type="ys.app.petproject.viewmodel.manage.EmployeeNameNewItemViewModel" />
    </data>

    <LinearLayout
        android:id="@+id/root_view"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:orientation="vertical">

        <Button
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginLeft="10dp"
            android:layout_marginRight="10dp"
            android:layout_marginTop="5dp"
            android:background="@drawable/selector_press_button_normal"
            android:onClick="@{itemViewModel.onClickButton}"
            android:text="@{itemViewModel.model.name}"
            android:textColor="@color/white"
            android:textSize="13sp" />

    </LinearLayout>
</layout>